"use client";

import { ScrollArea } from "@/components/ui/scroll-area";

import React, { useRef } from "react";
import * as Highcharts from "highcharts";
import { type HighchartsReact } from "highcharts-react-official";

export default function page() {
  return (
    <div className="container relative mx-auto box-border flex w-full justify-center overflow-hidden bg-midnightblue-200 leading-[normal] tracking-[normal]">
      <img className=" " loading="lazy" alt="" src="/hipertech-logo.svg" />
    </div>
  );
}
